/**
 * Plugin constants (hardcoded configuration) for shared code.
 */
export default class Gb_Shared_Defaults {
    NAME = 'ai_gamblingbot';

    constructor() {
        Object.freeze(this);
    }
}
